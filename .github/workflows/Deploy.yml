name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted  # Usa el runner auto-hospedado

    steps:
    - name: Checkout code
      uses: actions/checkout@v4


    - name: Deploy application
      run: |
        # Comandos para desplegar tu aplicación
        cd ../Fastapi-MySQL/WebPage
        git pull origin main
        pm2 reload all
         
